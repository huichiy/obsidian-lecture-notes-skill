# Mode: LINT — Health-check existing notes

⚠️ **This mode is a planned feature, not yet specified.**

If the user invokes LINT mode (by saying "lint my notes", "check OOAD notes", "audit my vault", "health check", etc.), respond:

> Lint mode is planned but the specific checks haven't been defined yet. Want to design the rules together now, or skip linting for this session?
>
> Planned checks may include: YAML schema violations, missing MOC backlinks, broken wikilinks, lectures with < 10 flashcards, exam-likely concepts not in cheat sheet, MOC missing lecture entries, concept naming inconsistencies.

Do not invent lint rules and start running them. Wait for the user to define what counts as a violation.
